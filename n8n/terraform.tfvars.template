# Required: point this host to the NLB public IP before issuing TLS certificates.
n8n_host = "n8n.example.com"

# Required: OCI Object Storage key for the email delivery Terraform state.
email_key = "email/terraform.tfstate"
